const serveHtmlForEnvironment = require('./serveHtmlForEnvironment')
const errorReducer = require('./errorReducer')
const emails = require('./emails')

module.exports = {
  serveHtmlForEnvironment,
  errorReducer,
  emails
}
